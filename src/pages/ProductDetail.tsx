import NavBar from "../components/NavBar";
import Wrapper from "../components/Wrapper";

const Products = () => {
  return (
    <Wrapper>
      <NavBar />
    </Wrapper>
  );
};

export default Products;